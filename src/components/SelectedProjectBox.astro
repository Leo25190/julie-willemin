---
import LinkItem from './LinkItem.astro';

const { projects = {} } = Astro.props;
---

<div class='flex flex-col p-1'>
  {projects.map((project) => <LinkItem href={project.link} label={project.name} />)}
</div>
